require 'spec_helper'

describe PictureFrame do
  it 'has a version number' do
    expect(PictureFrame::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
